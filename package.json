{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "clean:dev": "(yarn clean:nuxt || true) && nuxt dev",
    "clean:preview": "yarn clean:nuxt && nuxt build && nuxt preview",
    "clean:nuxt": "rm .nuxt .output -r || true",
    "clean:all": "rm .nuxt .output node_modules yarn.lock package-lock.json -r"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.1",
    "@vue/composition-api": "^1.7.0",
    "convert-time": "^0.3.0",
    "dayjs": "^1.11.5",
    "epic-spinners": "^1.1.0",
    "pinia": "^2.0.20",
    "tiny-timer": "^1.6.0",
    "vue-tippy": "^4.16.0"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.0.0-0",
    "@nuxtjs/tailwindcss": "^5.3.2",
    "nuxt": "3.0.0-rc.8",
    "vite-plugin-pwa": "^0.12.3"
  }
}
