{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "build:preview": "nuxt build && nuxt preview",
    "clean:all": "rm .nuxt .output node_modules yarn.lock -r",
    "clean:nuxt": "rm .nuxt .output -r || true",
    "dev": "nuxt dev",
    "dev:clean": "yarn clean:nuxt && nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.1",
    "@vue/composition-api": "^1.7.0",
    "convert-time": "^0.3.0",
    "dayjs": "^1.11.5",
    "pinia": "^2.0.20",
    "tiny-timer": "^1.6.0"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.0.0-0",
    "@nuxtjs/tailwindcss": "^5.3.2",
    "nuxt": "3.0.0-rc.8",
    "vite-plugin-pwa": "^0.12.3"
  }
}
